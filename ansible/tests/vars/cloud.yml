---
platform_name: "cloud"
grub_hardening_expected: false
time_sync_backend: "chrony"
platform_expected_admin_user: "packer"
admin_sudo_group_required: false


required_separate_mounts: []

required_mount_options:
  /dev/shm:
    - nodev
    - nosuid
    - noexec

  /proc:
    - nodev
    - nosuid
    - noexec
